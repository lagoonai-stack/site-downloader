node_modules/
.env
.env.local
*.log

# Pastas-fonte da galeria (310MB) - so o subconjunto curado em public/
# (thumbs/, previews/, catalog.json) e commitado.
/galeria_arquivo/
/brabo_design_system/

# Docs internos pra quem for mexer no front (brabo-academy) - repo e
# publico, entao ficam so localmente/no Artifact compartilhado direto com
# o time, nunca no historico do git.
/API.md
/FRONTEND_HANDOFF.md
